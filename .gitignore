# ── Dependencies & build ──────────────────────────────────────────
node_modules/
.next/
/out/
/build

# ── Env / secrets ─────────────────────────────────────────────────
.env
.env.local
.env*.local
**/credentials.json
**/token.json
outreach/.gmail-secrets/

# ── Generated / local-only ────────────────────────────────────────
.cache/
data/
data/raw/
*.tsbuildinfo
next-env.d.ts

# ── OS / editor / logs ────────────────────────────────────────────
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
uat-*.json
uat-*.log
~$*.docx

# Leftover nested copy (do not commit)
/Echoes/

# ── Not needed to run the app on Vercel ───────────────────────────
# Business / planning / outreach stay local; keep root README.md
# and any README.md under scripts/personas that document the product.
Planning Documents/
ECHOES_MVP_Plan.md
beta-launch/
outreach/

# Outreach automation (local only)
scripts/gmail-drafts/

# Heavy / non-runtime docs & assets
*.pdf
*.docx
*.doc

# Cursor / agent local state (if ever present under the repo)
.cursor/
agent-transcripts/
